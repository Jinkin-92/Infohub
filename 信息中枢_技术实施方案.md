# 个人信息中枢 · 技术实施方案

> MVP 阶段：本地 Docker Compose 部署，覆盖知乎 / X / 新闻（含 waytoagi）三个平台

---

## 一、最终需求决策总表

| 维度 | 决策 |
|------|------|
| 部署方式 | 本地 Docker Compose（全部服务）|
| 前端 | Next.js 14 (App Router) + Tailwind CSS |
| 后端 | Node.js + Hono |
| 数据库 | PostgreSQL 16 |
| RSS 适配层 | RSSHub（自建容器）|
| 公众号采集 | werss（独立 docker-compose，MVP 暂不接入）|
| 财经数据 | Yahoo Finance API（MVP 暂不接入）|
| 内置新闻源管理 | 设置页可增删任意 RSS URL，含预设开关 |
| 导航结构 | 按平台分 Tab，新闻含 waytoagi（颜色圆点区分）|
| 内容深度 | 标题+摘要；视频类跳转，文字类支持展开 |
| 已读状态 | 存服务端 PostgreSQL，跨设备同步 |
| Agent 接口 | REST API 返回 JSON |
| MVP 第一批平台 | 知乎、X、新闻（含 waytoagi）|
| 后续扩展平台 | B站、YouTube（方案A自动解析 handle）、公众号 |

---

## 二、本地服务架构

```
localhost:3000   → 前端 Next.js
localhost:3001   → 后端 API (Hono)
localhost:5432   → PostgreSQL
localhost:1200   → RSSHub

（后续独立）
localhost:8001   → werss（微信公众号，MVP 暂不启动）
```

所有服务通过一个 `docker-compose.yml` 管理，werss 单独一个 `docker-compose.werss.yml`。

---

## 三、项目目录结构

```
infohub/
├── docker-compose.yml          # 主服务编排
├── docker-compose.werss.yml    # 微信公众号服务（后续）
├── .env                        # 环境变量
│
├── frontend/                   # Next.js 前端
│   ├── app/
│   │   ├── page.tsx            # 主页（瀑布流）
│   │   ├── settings/
│   │   │   └── page.tsx        # 订阅源管理
│   │   └── layout.tsx
│   ├── components/
│   │   ├── FeedList.tsx        # 瀑布流列表
│   │   ├── FeedItem.tsx        # 单条内容卡片
│   │   ├── TabBar.tsx          # 平台 Tab 导航
│   │   ├── FinancePanel.tsx    # 财经面板（后续）
│   │   └── AddSourceModal.tsx  # 添加订阅源弹窗
│   ├── lib/
│   │   ├── api.ts              # 后端 API 调用封装
│   │   └── types.ts            # 类型定义
│   └── package.json
│
├── backend/                    # Hono 后端
│   ├── src/
│   │   ├── index.ts            # 入口，路由注册
│   │   ├── routes/
│   │   │   ├── feed.ts         # /api/feed 相关
│   │   │   ├── sources.ts      # /api/sources 订阅源管理
│   │   │   ├── market.ts       # /api/market 财经（后续）
│   │   │   └── agent.ts        # /api/agent 对外接口
│   │   ├── services/
│   │   │   ├── collector.ts    # 采集调度主逻辑
│   │   │   ├── parsers/
│   │   │   │   ├── zhihu.ts    # 知乎解析器
│   │   │   │   ├── twitter.ts  # X 解析器
│   │   │   │   ├── youtube.ts  # YouTube 解析器（后续）
│   │   │   │   ├── bilibili.ts # B站解析器（后续）
│   │   │   │   └── rss.ts      # 通用 RSS 解析器
│   │   │   └── urlDetector.ts  # 用户输入 URL 平台识别
│   │   ├── db/
│   │   │   ├── client.ts       # PostgreSQL 连接（postgres.js）
│   │   │   ├── schema.sql      # 初始化 SQL
│   │   │   └── queries.ts      # 常用查询封装
│   │   └── cron.ts             # 定时任务（node-cron）
│   ├── package.json
│   └── tsconfig.json
│
└── rsshub/
    └── config.js               # RSSHub 自定义配置（可选）
```

---

## 四、docker-compose.yml

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: infohub
      POSTGRES_USER: infohub
      POSTGRES_PASSWORD: infohub123
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/src/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U infohub"]
      interval: 5s
      timeout: 5s
      retries: 5

  rsshub:
    image: diygod/rsshub:latest
    ports:
      - "1200:1200"
    environment:
      NODE_ENV: production
      CACHE_TYPE: memory
      # 需要 Cookie 的平台在这里填写
      # ZHIHU_COOKIES: ""
      # TWITTER_COOKIE: ""
    restart: unless-stopped

  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgresql://infohub:infohub123@postgres:5432/infohub
      RSSHUB_URL: http://rsshub:1200
      PORT: 3001
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:3001
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
```

---

## 五、数据库 Schema

```sql
-- 订阅源表
CREATE TABLE sources (
  id          SERIAL PRIMARY KEY,
  name        VARCHAR(100) NOT NULL,
  platform    VARCHAR(30)  NOT NULL,
  -- 'zhihu' | 'bilibili' | 'youtube' | 'x' | 'wechat' | 'news' | 'custom'
  input_url   TEXT NOT NULL,             -- 用户原始输入的 URL
  rss_url     TEXT NOT NULL,             -- 解析后实际拉取的 RSS URL
  platform_id VARCHAR(100),             -- 提取出的平台用户 ID
  fetch_interval_min INT DEFAULT 360,   -- 采集间隔（分钟）
  enabled     BOOLEAN DEFAULT TRUE,
  last_fetched_at TIMESTAMPTZ,
  created_at  TIMESTAMPTZ DEFAULT NOW()
);

-- 内容条目表
CREATE TABLE items (
  id           BIGSERIAL PRIMARY KEY,
  source_id    INT REFERENCES sources(id) ON DELETE CASCADE,
  guid         TEXT NOT NULL,             -- RSS item 的 guid，用于去重
  title        TEXT NOT NULL,
  summary      TEXT,
  url          TEXT NOT NULL,
  author       VARCHAR(100),
  cover_url    TEXT,
  platform     VARCHAR(30) NOT NULL,
  published_at TIMESTAMPTZ NOT NULL,
  fetched_at   TIMESTAMPTZ DEFAULT NOW(),
  raw_json     JSONB,                     -- 原始数据，供 Agent 使用
  UNIQUE(source_id, guid)
);

CREATE INDEX idx_items_published_at ON items(published_at DESC);
CREATE INDEX idx_items_platform     ON items(platform);
CREATE INDEX idx_items_source_id    ON items(source_id);

-- 已读状态表（服务端同步）
CREATE TABLE read_status (
  item_id    BIGINT REFERENCES items(id) ON DELETE CASCADE,
  read_at    TIMESTAMPTZ DEFAULT NOW(),
  PRIMARY KEY (item_id)
);

-- 财经快照表（后续接入）
CREATE TABLE market_snapshots (
  id          SERIAL PRIMARY KEY,
  symbol      VARCHAR(20) NOT NULL,      -- 'SPX' | 'NDX' | 'GOLD' | 'BRENT'
  trade_date  DATE NOT NULL,
  open        NUMERIC(12,4),
  high        NUMERIC(12,4),
  low         NUMERIC(12,4),
  close       NUMERIC(12,4),
  change_pct  NUMERIC(6,4),
  commentary  TEXT,
  ohlc_json   JSONB,
  created_at  TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(symbol, trade_date)
);

-- 预设内置新闻源（初始数据）
INSERT INTO sources (name, platform, input_url, rss_url, fetch_interval_min) VALUES
  ('36氪 · AI', 'news', 'https://36kr.com/information/AI', 'https://36kr.com/feed', 60),
  ('Hacker News', 'news', 'https://news.ycombinator.com', 'https://hnrss.org/frontpage', 60),
  ('The Verge · AI', 'news', 'https://www.theverge.com/ai', 'https://www.theverge.com/rss/ai-artificial-intelligence/index.xml', 60),
  ('waytoagi', 'news', 'https://www.waytoagi.com/zh/blog', 'https://www.waytoagi.com/zh/blog', 120);
-- 注：waytoagi 无原生 RSS，rss_url 字段存原始页面 URL，采集器识别后走爬虫逻辑
```

---

## 六、核心后端代码骨架

### 6.1 URL 平台识别器 (`urlDetector.ts`)

```typescript
export interface DetectionResult {
  platform: string;
  platformId: string;
  rssUrl: string;
  displayName: string;
}

const RSSHUB = process.env.RSSHUB_URL || 'http://localhost:1200';

export async function detectSource(inputUrl: string): Promise<DetectionResult> {
  const url = new URL(inputUrl);
  const host = url.hostname.replace('www.', '');
  const path = url.pathname;

  // 知乎用户：zhihu.com/people/mr-dang-77
  if (host === 'zhihu.com' && path.startsWith('/people/')) {
    const id = path.split('/people/')[1].split('/')[0];
    return {
      platform: 'zhihu',
      platformId: id,
      rssUrl: `${RSSHUB}/zhihu/people/activities/${id}`,
      displayName: `知乎 · ${id}`,
    };
  }

  // X 用户：x.com/sama 或 twitter.com/sama
  if (host === 'x.com' || host === 'twitter.com') {
    const id = path.split('/')[1];
    return {
      platform: 'x',
      platformId: id,
      rssUrl: `${RSSHUB}/twitter/user/${id}`,
      displayName: `X · @${id}`,
    };
  }

  // B站 UP 主：space.bilibili.com/12345678
  if (host === 'bilibili.com' || host === 'space.bilibili.com') {
    const uid = path.replace('/', '').split('/')[0];
    return {
      platform: 'bilibili',
      platformId: uid,
      rssUrl: `${RSSHUB}/bilibili/user/video/${uid}`,
      displayName: `B站 · UID ${uid}`,
    };
  }

  // YouTube handle：youtube.com/@AndrejKarpathy
  if (host === 'youtube.com') {
    const handle = path.split('/')[1]; // '@AndrejKarpathy'
    const channelId = await resolveYouTubeChannelId(inputUrl);
    return {
      platform: 'youtube',
      platformId: channelId,
      rssUrl: `https://www.youtube.com/feeds/videos.xml?channel_id=${channelId}`,
      displayName: `YouTube · ${handle}`,
    };
  }

  // waytoagi（特殊标记，走爬虫）
  if (host === 'waytoagi.com') {
    return {
      platform: 'news',
      platformId: 'waytoagi',
      rssUrl: inputUrl,
      displayName: 'waytoagi',
    };
  }

  // 其他：当作自定义 RSS URL 直接使用
  return {
    platform: 'custom',
    platformId: '',
    rssUrl: inputUrl,
    displayName: new URL(inputUrl).hostname,
  };
}

// YouTube handle → channel_id（方案A：自动解析）
async function resolveYouTubeChannelId(pageUrl: string): Promise<string> {
  const res = await fetch(pageUrl, {
    headers: { 'User-Agent': 'Mozilla/5.0' }
  });
  const html = await res.text();
  const match = html.match(/"channelId":"(UC[^"]+)"/);
  if (!match) throw new Error('无法解析 YouTube channel_id，请稍后重试');
  return match[1];
}
```

### 6.2 通用 RSS 采集器 (`services/collector.ts`)

```typescript
import Parser from 'rss-parser';
import * as db from '../db/queries';

const parser = new Parser({
  customFields: { item: ['description', 'content:encoded', 'media:thumbnail'] }
});

export async function collectSource(sourceId: number) {
  const source = await db.getSourceById(sourceId);
  if (!source?.enabled) return;

  // waytoagi 走爬虫分支
  if (source.platform_id === 'waytoagi') {
    return collectWaytoagi(sourceId);
  }

  try {
    const feed = await parser.parseURL(source.rss_url);
    const items = feed.items.map(item => ({
      source_id:    sourceId,
      guid:         item.guid || item.link || item.title || '',
      title:        item.title || '无标题',
      summary:      stripHtml(item.contentSnippet || item.description || ''),
      url:          item.link || '',
      author:       item.creator || item.author || source.name,
      cover_url:    extractCover(item),
      platform:     source.platform,
      published_at: item.isoDate ? new Date(item.isoDate) : new Date(),
      raw_json:     item,
    }));

    await db.upsertItems(items);
    await db.updateSourceFetchedAt(sourceId);
  } catch (err) {
    console.error(`[collector] source ${sourceId} 采集失败:`, err);
  }
}

// waytoagi 专用爬虫
async function collectWaytoagi(sourceId: number) {
  const res = await fetch('https://www.waytoagi.com/zh/blog', {
    headers: { 'User-Agent': 'Mozilla/5.0' }
  });
  const html = await res.text();
  // 解析文章列表（具体选择器待实际页面结构确定）
  const articles = parseWaytoagiHtml(html);
  await db.upsertItems(articles.map(a => ({ ...a, source_id: sourceId, platform: 'news' })));
  await db.updateSourceFetchedAt(sourceId);
}

function stripHtml(html: string): string {
  return html.replace(/<[^>]+>/g, '').trim().slice(0, 300);
}

function extractCover(item: any): string | null {
  return item['media:thumbnail']?.$.url
    || item.enclosure?.url
    || null;
}
```

### 6.3 定时任务 (`cron.ts`)

```typescript
import cron from 'node-cron';
import * as db from './db/queries';
import { collectSource } from './services/collector';

export function startCron() {
  // 每 30 分钟检查一次，按各源的 fetch_interval 决定是否实际触发
  cron.schedule('*/30 * * * *', async () => {
    const sources = await db.getSourcesDueForFetch();
    console.log(`[cron] 本轮待采集: ${sources.length} 个源`);
    for (const source of sources) {
      await collectSource(source.id);
    }
  });

  console.log('[cron] 定时任务已启动');
}
```

### 6.4 Feed API 路由 (`routes/feed.ts`)

```typescript
import { Hono } from 'hono';
import * as db from '../db/queries';

const feed = new Hono();

// 获取条目列表
// GET /api/feed?platform=zhihu&limit=20&offset=0&unread_only=false
feed.get('/', async (c) => {
  const { platform, limit = '20', offset = '0', unread_only } = c.req.query();
  const items = await db.getItems({
    platform: platform || undefined,
    limit: parseInt(limit),
    offset: parseInt(offset),
    unreadOnly: unread_only === 'true',
  });
  return c.json({ items, total: items.length });
});

// 标记已读
// POST /api/feed/read  body: { item_id: number }
feed.post('/read', async (c) => {
  const { item_id } = await c.req.json();
  await db.markAsRead(item_id);
  return c.json({ ok: true });
});

// 批量标记已读
// POST /api/feed/read-all  body: { platform?: string }
feed.post('/read-all', async (c) => {
  const { platform } = await c.req.json();
  await db.markAllAsRead(platform);
  return c.json({ ok: true });
});

export default feed;
```

### 6.5 订阅源管理路由 (`routes/sources.ts`)

```typescript
import { Hono } from 'hono';
import { detectSource } from '../services/urlDetector';
import * as db from '../db/queries';

const sources = new Hono();

// 获取所有订阅源
feed.get('/', async (c) => {
  const list = await db.getAllSources();
  return c.json(list);
});

// 添加订阅源（用户粘贴任意 URL）
// POST /api/sources  body: { url: string }
sources.post('/', async (c) => {
  const { url } = await c.req.json();
  try {
    const detected = await detectSource(url);
    const source = await db.createSource({
      name:        detected.displayName,
      platform:    detected.platform,
      input_url:   url,
      rss_url:     detected.rssUrl,
      platform_id: detected.platformId,
      fetch_interval_min: detected.platform === 'news' ? 60 : 360,
    });
    // 立即触发一次采集
    collectSource(source.id).catch(console.error);
    return c.json({ ok: true, source, detected });
  } catch (err: any) {
    return c.json({ ok: false, error: err.message }, 400);
  }
});

// 启用/停用订阅源
// PATCH /api/sources/:id  body: { enabled: boolean }
sources.patch('/:id', async (c) => {
  const id = parseInt(c.req.param('id'));
  const { enabled } = await c.req.json();
  await db.updateSource(id, { enabled });
  return c.json({ ok: true });
});

// 删除订阅源
sources.delete('/:id', async (c) => {
  const id = parseInt(c.req.param('id'));
  await db.deleteSource(id);
  return c.json({ ok: true });
});

export default sources;
```

### 6.6 Agent 接口 (`routes/agent.ts`)

```typescript
import { Hono } from 'hono';
import * as db from '../db/queries';

const agent = new Hono();

// 每日简报数据
// GET /api/agent/daily?date=2025-03-24
agent.get('/daily', async (c) => {
  const date = c.req.query('date') || new Date().toISOString().split('T')[0];
  const [items, unreadCount] = await Promise.all([
    db.getItemsByDate(date),
    db.getUnreadCount(),
  ]);
  return c.json({
    date,
    total: items.length,
    unread: unreadCount,
    items: items.map(i => ({
      id:           i.id,
      platform:     i.platform,
      author:       i.author,
      title:        i.title,
      summary:      i.summary,
      url:          i.url,
      published_at: i.published_at,
      is_read:      i.is_read,
    })),
  });
});

// 按平台/条件查询
// GET /api/agent/items?platform=news&limit=20&unread_only=true
agent.get('/items', async (c) => {
  const { platform, limit = '20', unread_only } = c.req.query();
  const items = await db.getItems({
    platform: platform || undefined,
    limit: parseInt(limit),
    unreadOnly: unread_only === 'true',
  });
  return c.json({ items });
});

// 所有订阅源配置
agent.get('/sources', async (c) => {
  const list = await db.getAllSources();
  return c.json(list);
});

export default agent;
```

---

## 七、订阅源输入规则总表

用户只需粘贴浏览器地址栏的 URL，系统自动识别：

| 平台 | 粘贴示例 | 自动提取 | 转换后 RSS |
|------|---------|---------|-----------|
| 知乎 | `zhihu.com/people/mr-dang-77` | ID: `mr-dang-77` | `rsshub/zhihu/people/activities/mr-dang-77` |
| X | `x.com/sama` | ID: `sama` | `rsshub/twitter/user/sama` |
| B站 | `space.bilibili.com/12345678` | UID: `12345678` | `rsshub/bilibili/user/video/12345678` |
| YouTube | `youtube.com/@AndrejKarpathy` | 自动解析 channel_id | 原生 YouTube RSS |
| 公众号 | 无需输入，werss 界面搜索添加 | mp_id | `werss/feeds/{mp_id}.xml` |
| 任意 RSS | 直接粘贴 RSS URL | — | 直接使用 |
| 内置新闻源 | 设置页勾选预设列表 | — | 预设 URL |
| waytoagi | 自动识别 `waytoagi.com` | — | 专用爬虫 |
| 财经 | 无需输入，系统内置 | — | Yahoo Finance API |

---

## 八、MVP 开发阶段规划

### Phase 1 · 基础骨架（第 1-2 周）

- [ ] Docker Compose 配置完成，`docker compose up` 一键启动
- [ ] PostgreSQL schema 初始化
- [ ] Hono 后端基础路由可通 (`/api/feed`, `/api/sources`)
- [ ] RSSHub 容器跑通，手动测试知乎 RSS 拉取
- [ ] 前端瀑布流基础布局 + Tab 导航
- [ ] 后端 Cron 定时采集跑通，数据写入 DB

**验证标准**：手动添加一个知乎用户，30 分钟内出现在前端列表中。

### Phase 2 · 核心交互（第 3 周）

- [ ] 已读变灰（前端 + 服务端同步）
- [ ] 文字类内容卡片展开/收起
- [ ] X 平台接入（配置 Twitter Cookie）
- [ ] 新闻 RSS 接入（36氪 + HN + Verge）
- [ ] waytoagi 爬虫完成

**验证标准**：三个平台均有内容，已读状态刷新页面后保持。

### Phase 3 · 订阅源管理（第 4 周）

- [ ] 添加订阅源弹窗（自动识别 + 预览）
- [ ] 设置页订阅源列表（启用/停用/删除）
- [ ] 内置新闻源预设列表 + 勾选开关
- [ ] Agent REST API 完整测试

**验证标准**：通过前端 UI 完成整个「粘贴 URL → 确认 → 出现内容」流程。

### Phase 4 · 后续扩展（按需）

- [ ] B站接入
- [ ] YouTube 接入（含 handle 自动解析）
- [ ] 财经数据面板（Yahoo Finance + K 线图）
- [ ] werss 接入（微信公众号）
- [ ] Agent 简报生成（调用 Claude API）

---

## 九、关键依赖版本

### 后端

```json
{
  "hono": "^4.x",
  "postgres": "^3.x",
  "rss-parser": "^3.x",
  "node-cron": "^3.x",
  "cheerio": "^1.x",
  "typescript": "^5.x"
}
```

### 前端

```json
{
  "next": "14.x",
  "react": "^18.x",
  "tailwindcss": "^3.x",
  "swr": "^2.x",
  "lightweight-charts": "^4.x"
}
```

---

## 十、启动命令速查

```bash
# 首次启动
git clone <repo>
cd infohub
docker compose up -d

# 查看日志
docker compose logs -f backend
docker compose logs -f rsshub

# 停止
docker compose down

# 重建（代码变更后）
docker compose up -d --build backend
docker compose up -d --build frontend

# 进入数据库
docker compose exec postgres psql -U infohub -d infohub

# 手动触发采集（调试用）
curl -X POST http://localhost:3001/api/sources/1/collect

# 本地 werss（后续微信公众号）
docker compose -f docker-compose.werss.yml up -d
```

---

*文档版本：MVP v1.0 · 2025-03-24*

---

## 十一、当前收尾状态（2026-03-26）

### 已确认完成

- 后端 TypeScript 构建通过，核心路由 `/api/feed`、`/api/sources`、`/api/tags` 可用
- 前端 Next.js 构建通过，首页 `http://localhost:3000` 可返回 `200`
- 前端 `/api` 代理已改为可配置，默认本地代理到 `http://localhost:3002`
- `docker-compose.yml` 已为前端补充 `API_PROXY_TARGET=http://backend:3001`，避免容器内错误访问 `localhost`
- SQLite 初始化逻辑已补充默认标签自愈与默认新闻源初始化
- 本地 `sqlite` 模式下，后端已支持在 `Node 25+` 环境自动回退到 `Node 24` 兼容启动

### 本轮实际修复

1. 修复后端查询层与类型层脱节问题，补齐 tags/feed/sources 相关链路
2. 修复前端生产启动时 `.next` 产物不稳定导致的 `500`，构建前会先清理旧产物
3. 统一 README、前端代理、后端实际端口之间的配置漂移
4. 完成一次非提交式 QA，验证首页、标签接口、源列表、Feed 列表、创建源接口

### 当前已知限制

- 本机默认 `Node 25.8.1` 直接运行 `better-sqlite3` 仍依赖本地原生工具链，因此当前采用“sqlite 模式自动使用 Node 24”作为兼容方案
- 手动测试 `https://hnrss.org/frontpage` 时返回过 `429`，说明外部 RSS 源仍可能触发限流，后续需要补重试与退避
- 当前 SQLite 数据库里如果已有旧测试数据，不会自动覆盖为全新演示数据；默认源初始化仅对空库生效

### 建议作为下一阶段起点的事项

- P0: 为采集器补充 429/网络异常的指数退避与更清晰的错误分类
- P0: 增加最小可用测试集，覆盖 `sources -> collect -> feed` 主路径
- P1: 将 README 与实施方案中的运行方式进一步统一，明确本地 `3002` / 容器内 `3001`
- P1: 评估是否继续保留 `better-sqlite3`，或改为对 `Node 25+` 更友好的 SQLite 方案

### 与原方案的偏差说明

- 文档原先默认后端本地端口为 `3001`，当前本地 `.env` 实际使用 `3002`
- 文档原先默认通过 Docker Compose 作为主要启动方式，但当前本机并未安装可用 Docker，因此本轮验证主要基于本地 Node 进程
- 原方案中标签管理仍标记为未完成；当前代码已具备基础标签 API 与前端交互能力，但仍缺系统化测试与完整验收
---

## 十二、MVP执行约束（2026-03-27 基线）

本节作为后续实现、测试、验收的唯一执行基线。除非你明确调整范围，否则不得偏离。

### 12.1 平台边界

MVP 只覆盖以下 6 类能力，严格按顺序推进：

1. 知乎
2. B站
3. X
4. YouTube
5. 新闻
6. 财经数据

以下内容不在当前 MVP 范围内，禁止因排障或“顺手支持”而新增到产品主链路：

- 微博
- 小红书
- 豆瓣
- 其他未在本节列出的平台
- 任意额外 cookie 平台接入

微信公众号属于目标平台，但当前阶段仍按原方案保持“暂不纳入 MVP 实现”，后续独立推进，不插队到上述顺序中。

### 12.2 总体原则

后续所有实现必须满足以下原则：

- 只打通一条链路，再进入下一条链路
- 当前链路未达到验收标准前，不并行扩展备选方案
- 优先采用无登录态、无 cookie、可长期维护的接入方式
- 如果某个平台缺少稳定方案，可以标记为“实验性 MVP”，但不得因此扩展非目标平台
- 不为了规避当前问题而新增新的平台、设置页能力或通用登录态能力

### 12.3 平台执行顺序与允许策略

#### 知乎

- 当前目标：仅支持 `people/<id>` 用户动态订阅
- 允许方案：最小可用的单一路径接入
- 暂不扩展：专栏、问题、收藏夹、话题、热榜
- 风险说明：知乎稳定性不足时，可标记为“实验性 MVP”，但仍需走完整的新增订阅、采集、展示、报错链路

#### B站

- 当前目标：仅支持公开 UP 主视频更新
- 允许方案：`space.bilibili.com/<uid>` -> 单一视频订阅链路
- 暂不扩展：直播、动态、专栏、番剧、收藏夹

#### X

- 当前目标：仅支持用户时间线
- 允许方案：`x.com/<user>` -> 单一用户订阅链路
- 暂不扩展：home timeline、likes、lists、search、keyword
- 约束：优先评估无 cookie 方案；若无法稳定成立，应明确标记实验性，而不是继续扩散实现

#### YouTube

- 当前目标：仅支持频道/handle 视频更新
- 允许方案：优先原生 RSS
- 暂不扩展：Shorts、Playlist、Live、Community

#### 新闻

- 当前目标：仅支持公开 RSS 新闻源
- 允许方案：原生 RSS 或明确稳定的公开 feed
- 暂不扩展：站点登录态抓取、通用网页模拟抓取、非必要站点定制爬虫

#### 财经数据

- 当前目标：仅支持最小行情数据面板
- 允许方案：结构化 API
- 禁止方案：RSSHub、cookie、网页登录态采集
- 暂不扩展：持仓、提醒、策略分析、评论生成

### 12.4 每条链路统一验收标准

每个平台都必须依次通过以下 5 项，才算“链路打通”：

1. 输入一个标准 URL，系统能正确识别平台与平台 ID
2. 订阅源能够创建成功，并写入正确的 `platform / input_url / rss_url / platform_id`
3. 手动触发采集时，后端返回明确结果：成功或可执行错误
4. 首页 Feed 能展示最小必要字段：标题、时间、链接、来源
5. 失败时前端展示明确错误原因，不能只显示“暂无内容”

### 12.5 暂停条件

出现以下任一情况时，必须暂停当前实现，先回到本方案确认，不得继续发散：

- 需要新增非目标平台才能“绕过当前问题”
- 需要增加通用 cookie 管理能力才能继续推进主链路
- 文档中的平台边界与代码中的平台边界开始不一致
- 当前链路已经偏离“最小可用”，开始出现多个并行接入方案
- 为解决单一平台问题而修改了其他平台的产品边界

### 12.6 执行与文档同步要求

后续执行采用固定节奏：

1. 先写清当前平台的“主方案、风险、验收标准、失败退出条件”
2. 再做实现
3. 再做手动测试
4. 再记录结果到本方案
5. 只有当当前平台达到验收标准后，才能进入下一平台

当前基线执行顺序固定为：

`知乎 -> B站 -> X -> YouTube -> 新闻 -> 财经数据`

后续所有测试、排障、文档更新均以本节为准。

---

## 十三、知乎链路主方案（当前执行中）

### 13.1 范围

当前只做知乎用户动态这一条最小链路：

- 输入：`https://www.zhihu.com/people/<id>`
- 识别结果：`platform = zhihu`
- 采集目标：`/zhihu/people/activities/<id>`

本阶段不扩展：

- 专栏
- 问题
- 收藏夹
- 话题
- 热榜

### 13.2 主方案

知乎链路当前只允许采用“公开可访问路径”的方案：

1. 用户输入知乎个人主页 URL
2. 后端识别出 `people/<id>`
3. 生成最小订阅源记录
4. 采集器尝试抓取公开活动流
5. 若成功，则进入首页 Feed 展示
6. 若失败，则前端展示明确错误，且不引导用户转向非 MVP 方案

当前明确禁止作为知乎 MVP 主方案的内容：

- 在产品主链路中依赖 cookie
- 为知乎新增通用登录态设置能力
- 为了知乎问题扩展微博/小红书/豆瓣等其他平台能力

### 13.3 当前已验证状态（2026-03-27）

已验证结果如下：

- URL 识别链路已具备：`zhihu.com/people/<id>` 可以识别为 `zhihu`
- 创建订阅链路已具备：订阅源可以写入 `sources`
- 前端失败展示链路已具备：空列表时会显示订阅源错误，而不是统一显示“暂无内容”
- 当前公开采集链路未打通：本机对知乎公开用户页和活动接口请求均返回 `403`
- RSSHub `zhihu/people/activities/:id` 实测返回 `403 Forbidden`
- RSSHub `zhihu/timeline` 实测返回 `401 Authorization Required`
- RSSHub `zhihu/hot` 可以返回内容，但该路由不属于当前 MVP 定义的“知乎用户动态订阅”范围
- 因此，知乎当前状态定义为：`实验性 MVP`

### 13.4 当前验收口径

知乎链路当前分为两个阶段验收：

阶段 A：链路正确

- 能识别 URL
- 能创建订阅源
- 能手动触发采集
- 能在前端展示明确错误

阶段 B：内容打通

- 至少一个公开知乎用户源能稳定采集出真实条目
- 首页能正常展示知乎条目

只有阶段 B 完成，知乎才算真正“打通”；在此之前，知乎只记为“实验性已接线、未稳定打通”。

### 13.5 失败退出条件

如果继续验证后仍满足以下情况，则暂停知乎功能扩展，不再继续发散实现：

- 公开页面抓取持续返回 `403`
- RSSHub 公开路由持续无法稳定返回内容
- 唯一可行方案变成 cookie 或登录态注入

一旦触发上述条件，知乎链路保持在“实验性 MVP”状态，随后按总顺序进入 B站链路。

### 13.6 当前结论

截至 2026-03-27，知乎链路已完成“阶段 A：链路正确”，但未完成“阶段 B：内容打通”。

当前结论如下：

- 知乎用户动态订阅链路在产品层面可创建、可触发、可报错
- 在当前无 cookie、无登录态注入的 MVP 约束下，未验证出稳定可用的公开采集方式
- 因此知乎在当前版本中保留为 `实验性 MVP`，不再继续扩展实现
- 后续按总顺序切换到 B站链路

---

## 十四、B站链路主方案（当前执行中）

### 14.1 范围

当前只做 B站公开 UP 主视频更新这一条最小链路：

- 输入：`https://space.bilibili.com/<uid>`
- 识别结果：`platform = bilibili`
- 采集目标：`/bilibili/user/video/<uid>`

本阶段不扩展：

- 直播
- 动态
- 专栏
- 番剧
- 收藏夹

### 14.2 主方案

B站链路当前只允许采用公开可访问方案：

1. 用户输入 UP 主空间 URL
2. 后端识别 `space.bilibili.com/<uid>`
3. 创建最小订阅源记录
4. 采集器抓取公开视频更新流
5. 若成功，则进入首页 Feed 展示
6. 若失败，则记录明确错误，不引入 cookie 方案

### 14.3 当前已验证状态（2026-03-27）

已验证结果如下：

- URL 识别链路已具备：`space.bilibili.com/<uid>` 可识别为 `bilibili`
- 创建订阅链路已具备：订阅源可以正常写入 `sources`
- 本地 RSSHub 对 `bilibili/user/video/<uid>` 路由存在可访问能力，说明链路具备继续验证价值
- 当前主要阻塞不在 cookie，而在公开接口风控
- 直接访问 B站公开空间页可返回 `200`
- 直接访问公开视频列表 API 实测返回 `code = -352`，即“风控校验失败”

### 14.4 当前判断

截至当前，B站链路尚未完成“内容稳定打通”。

当前结论如下：

- B站比知乎更接近可打通状态
- 主要问题是公开视频 API 风控，而不是登录态缺失
- 因此 B站链路继续保留在主顺序中，下一步优先围绕“公开链路抗风控稳定性”继续验证
---

## 十六、2026-03-27 追加进展

### 16.1 B站链路最新状态

- 已新增“公开空间页 + 无 cookie 浏览器采集”的 B站专用采集器，不再依赖 RSSHub 的 `user/video` 路由。
- 真实验证样本：`https://space.bilibili.com/777536`
- 手动采集接口：`POST /api/sources/4/collect`
- 最新结果：`success = true, itemCount = 40`
- Feed 接口：`GET /api/feed?platform=bilibili&limit=5` 已返回真实 B站条目
- 当前结论：B站已经完成“阶段 A：链路正确”和“阶段 B：内容打通”，可进入后续闭环测试队列

### 16.2 知乎链路最新状态

- 使用无 cookie 浏览器级访问重新验证：
  - `https://www.zhihu.com/people/mr-dang-77`
  - `https://www.zhihu.com/people/mr-dang-77/activities`
- 结果一致：
  - 页面标题为“安全验证 - 知乎”
  - 页面正文为“请您登录后查看更多专业优质内容”
  - 网络层返回 `403`，随后跳转到 `account/unhuman`
- 当前结论：
  - 问题已经不是 RSSHub 选型或普通 HTTP 请求方式问题
  - 在“只支持 `people/<id>` 且保持无 cookie”的 MVP 约束下，知乎内容链路仍未打通
  - 因此知乎暂时仍保持“实验性 MVP / 未完成阶段 B”

## 十七、2026-03-27 知乎 Cookie 验证结果

- 通过本地 RSSHub 注入知乎登录态后，`/zhihu/people/activities/mr-dang-77` 已经能够返回真实动态内容。
- 后端采集器已补齐对 RSSHub JSON 响应的解析兼容，不再只接受 XML/RSS。
- 手动采集接口：
  - `POST /api/sources/2/collect`
  - 返回 `success = true, itemCount = 9`
- Feed 接口：
  - `GET /api/feed?platform=zhihu&limit=5`
  - 已返回真实知乎条目
- 当前结论：
  - 知乎 `people/<id>` 链路在“使用用户提供的 Cookie”前提下已完成内容打通
  - B站与知乎两条链路现在都已达到可进入闭环测试的状态
